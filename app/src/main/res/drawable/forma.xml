<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo -->
    <solid android:color="#ADDA93" />

    <!-- Bordes redondeados -->
    <corners android:radius="30dp" />

    <!-- Borde opcional -->
    <stroke
        android:width="1dp"
        android:color="#C0C0C0" />

    <!-- Padding interno -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp" />

</shape>
